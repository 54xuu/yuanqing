---
description: 跨应用记忆规约（Cursor）
alwaysApply: true
---

# 跨应用记忆规约（Cursor）

云端 **源清（yuanqing）** 是唯一记忆真源。通过 MCP 工具 `recall_memory` / `save_memory` 读写，与 OpenCode 等其它客户端共享同一份记忆。

本端标识：`tool=cursor`，写回时 `source_app=cursor`。

## 三层模型

| 层 | scope | 适用 | 路径约定 |
|----|-------|------|----------|
| 全局 | `global` | 与项目/工具无关的偏好（输出语言、编码规范等） | `全局记忆/<主题>` |
| 工具 | `tool` | 本工具或其它工具的特有踩坑/写法 | `工具记忆/<tool>/<主题>` |
| 项目 | `project` | 当前仓库独有约定 | `项目记忆/<项目名>/<主题>` |

**只要在上述目录下的笔记都属于记忆**（含网页手动创建），会被 `recall_memory` 召回。

## 会话开始 / 切换任务

在开始实质性工作前，调用：

```
recall_memory({ tool: "cursor", project: "<当前仓库名>" })
```

- `project` 取当前 git 仓库目录名（本仓为 `yuanqing`）。
- 将返回的 `global` ∪ `tool` ∪ `project` 纳入上下文并遵守。

## 沉淀记忆

在以下时机调用 `save_memory`（直接改云端，勿只写本地规则文件代替）：

1. 用户明确表达**持久偏好** → `scope=global`
2. 发现 **Cursor / OpenCode 工具特有**的行为或规避方式 → `scope=tool`，并填 `tool`
3. 确立 **本仓库** 约定或踩坑 → `scope=project`，`project=<仓库名>`

## 安全

- **记忆正文不入 git**；仅云端 SQLite + MCP 访问。
- MCP 配置（含 API Key）放在 `~/.cursor/mcp.json`，已加入 `.gitignore` 模式，勿提交仓库。

## Skill 同步

无需专用 download skill：对 Agent 说「把 `yuanqing-recall-memory` 下载到 `~/.cursor/skills`」，Agent 调用 MCP `download_skill` 并写入本地文件。
